<Window x:Class="hud_merger.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hud_merger"
        mc:Ignorable="d"
        Title="Settings"
        Width="700"
        SizeToContent="Height"
        Icon="favicon.ico"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label FontSize="22" Grid.Row="0" Margin="10,0,0,10">Settings</Label>
        <StackPanel Name="SettingsContainer" VerticalAlignment="Center" Grid.Row="1" Margin="10,0,10,0">
        </StackPanel>
        <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="2" Margin="10,5,10,10">
            <Button Style="{DynamicResource EnabledButton}" Padding="20,5" Margin="0,0,10,0" Click="CancelButton_Click">Cancel</Button>
            <Button Style="{DynamicResource AccentButton}" Padding="30,10" Click="ApplyButton_Click">Apply</Button>
        </WrapPanel>
    </Grid>
</Window>
