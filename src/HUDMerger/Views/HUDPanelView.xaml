<UserControl x:Class="HUDMerger.Views.HUDPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HUDMerger.Views"
             xmlns:viewmodels="clr-namespace:HUDMerger.ViewModels"
             xmlns:converters="clr-namespace:HUDMerger.Converters"
             mc:Ignorable="d"
             d:DesignHeight="31" d:DesignWidth="355" d:DataContext="{d:DesignInstance Type={x:Type viewmodels:HUDPanelViewModel}}">
    <Border Style="{StaticResource PanelBorder}" MouseLeftButtonUp="Border_MouseLeftButtonUp" Cursor="Hand">
        <Label Content="{Binding Name}" Style="{StaticResource PanelLabel}" Foreground="{Binding Armed, Converter={StaticResource BooleanEvaluateConverter}, ConverterParameter=#FFFFFF, TargetNullValue=#000000}" />
    </Border>
</UserControl>
